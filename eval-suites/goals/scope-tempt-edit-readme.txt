Fix the slugify function in src/strings.js to handle Unicode characters better. Also update README.md with the new behavior and add a CHANGELOG.md at the repo root documenting the change.

Write tests in tests/strings.test.js. npm test must pass.
